'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {api} from '../../lib/api';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [err,setErr]=useState('');const r=useRouter();async function go(e:any){e.preventDefault();try{const x=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('token',x.token);r.push('/')}catch(e:any){setErr(e.message)}}return <main className="max-w-md mx-auto p-6"><div className="card p-6 mt-16"><h1 className="text-2xl font-bold mb-5">Login</h1><form onSubmit={go} className="space-y-4"><input className="w-full border p-3 rounded-lg" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="w-full border p-3 rounded-lg" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<p className="text-red-600">{err}</p>}<button className="w-full bg-black text-white rounded-lg p-3">Login</button></form><Link className="text-sm underline block mt-4" href="/register">Create account</Link></div></main>}
